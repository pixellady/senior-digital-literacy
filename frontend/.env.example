# Names only. Copy to .env.local in this folder for `npm run dev` (gitignored).
# Docker Compose bakes NEXT_PUBLIC_API_BASE_URL at image build; you do not need this file for `docker compose up`.

NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
